apiVersion: apps/v1
kind: Deployment
metadata:
  name: competitor-worker
  labels:
    app: agentpro
    component: competitor-worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: agentpro
      component: competitor-worker
  template:
    metadata:
      labels:
        app: agentpro
        component: competitor-worker
    spec:
      containers:
        - name: competitor-processor
          image: your-registry/agentpro:latest
          command: ["npm", "run", "worker"]
          envFrom:
            - secretRef:
                name: agent-proseo-secrets
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            exec:
              command:
                - node
                - -e
                - "process.exit(0)"
            initialDelaySeconds: 30
            periodSeconds: 60